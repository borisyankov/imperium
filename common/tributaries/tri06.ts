import { Card, Suit } from '../../types';

const olmecs: Card = {
  cardName: 'Olmecs',
  effect: 'Free play. Return an {exhaust} to your state card.',
  suit: Suit.TRIBUTARY, 
  playerCount: 3,
  cardNumber: '1TRI6/11',
  vp: 3,
};

export default olmecs;