import { Card, Suit } from '../../types';

const hittites: Card = {
  cardName: 'Hittites',
  effect: 'Break through for {region}. Put this card into your history.',
  suit: Suit.TRIBUTARY, 
  cardNumber: '1TRI11/11',
  vp: 3,
};

export default hittites;