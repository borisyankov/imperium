import { Card, State } from '../types';

const advance: Card = {
  cardName: 'Advance',
  state: State.BARBARIAN,
  effect: 'Choose: pay 3 {materials} to acquire {uncivilised} / {civilised} OR pay 5 {materials} to break through for {uncivilised} / {civilised}.'
};

export default advance;