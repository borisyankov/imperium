import { Card, Icon, Nation } from '../../types';

const phalanx: Card = {
  cardName: 'Phalanx',
  icon: Icon.PINNED,
  effect: 'Exhaust: play a {region} from your discard pile (spending an action as normal).',
  nation: Nation.MACEDONIANS,
  cardNumber: '1MAC17/23',
};

export default phalanx;