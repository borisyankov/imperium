import { Card } from '../types';

const prosperity: Card = {
  cardName: 'Prosperity',
  effect: 'All players MAY draw 1 card. Choose: gain 1 {materials} per {production} OR gain 1 {population} per {region} you have in play.',
};

export default prosperity;