import { Card, Nation } from '../../types';

const druids: Card = {
  cardName: 'Druids',
  effect: 'Choose: gain 1 {progress} OR pay 1 {population} to break through for {uncivilised} OR return an {unrest} to gain 1 {population}.',
  nation: Nation.CELTS,
  cardNumber: '1CEL14/28',
};

export default druids;